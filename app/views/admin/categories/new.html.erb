<%= render partial: "shared/hero", locals: { title: "Admin", subtitle: "New Category" } %>
<div class="container">
  <%= simple_form_for [:admin, @category], class: "form-horizontal" do |f| %>
    <%= f.input :name, input_html: { required: true } %>
    <%= f.button :submit %>
  <% end %>

  <%= link_to "Back to Category List", admin_categories_path %>
</div>